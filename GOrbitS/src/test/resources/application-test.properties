spring.datasource.url=jdbc:h2:mem:test;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.hibernate.ddl-auto=create-drop

app.security.jwt.secret=test-jwt-secret-key-at-least-32-chars-long!

# Menos ruido en consola durante @SpringBootTest (semillas sandbox ya conocidas)
logging.level.biblioteca.gorbits.config.SeedUsersInitializer=WARN

package biblioteca.gorbits.catalog;

/**
 * UNITARIO: un ítem cobrado tal cual.
 * PAQUETE: precio unificado que corresponde a más de un ítem físico (p. ej. libro + regalo).
 */
public enum BookType {
    UNITARIO,
    PAQUETE
}
